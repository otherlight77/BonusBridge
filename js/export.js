export function exportJSON(filename,data){const blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const link=document.createElement('a');link.href=url;link.download=filename;link.click();setTimeout(()=>URL.revokeObjectURL(url),500)}
export async function importJSON(file){if(!file||file.type&&file.type!=='application/json')throw new Error('Choose a JSON file.');const value=JSON.parse(await file.text());if(!value||typeof value!=='object'||Array.isArray(value))throw new Error('The imported JSON root must be an object.');return value}

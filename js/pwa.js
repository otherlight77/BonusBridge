let installPrompt=null;
export function setupPWA({onNetworkChange,onInstallReady}={}){if('serviceWorker'in navigator&&location.protocol!=='file:')addEventListener('load',()=>navigator.serviceWorker.register('./sw.js').catch(()=>{}));const sync=()=>onNetworkChange?.(!navigator.onLine);addEventListener('online',sync);addEventListener('offline',sync);sync();addEventListener('beforeinstallprompt',event=>{event.preventDefault();installPrompt=event;onInstallReady?.(true)})}
export async function installApp(){if(!installPrompt)return false;installPrompt.prompt();const result=await installPrompt.userChoice;installPrompt=null;return result.outcome==='accepted'}
